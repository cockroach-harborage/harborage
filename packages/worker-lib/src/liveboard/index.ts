/**
 * Live board (ARCHITECTURE §6). Pure modules only: no Durable Object, no
 * bindings, no ambient state. The LiveBoard class is a thin wrapper, which is
 * what lets the red-line properties be tested directly.
 */
export * from './params.ts';
