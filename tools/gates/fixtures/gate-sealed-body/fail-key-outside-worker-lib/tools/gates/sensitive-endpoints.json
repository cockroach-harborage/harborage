{
	"endpoints": [
		{
			"endpoint": "POST /api/things/register",
			"class": "SEALED-TO-PLATFORM",
			"sealed_object": "thing-metadata-envelope",
			"note": "Broken invariant: an unseal-shaped binding is declared in a DIFFERENT package's Env contract. The scan used to look at one hardcoded path, so a second Env contract in a new package would have hidden a platform key without anyone noticing.",
			"platform_key": {
				"binding": "INTAKE_PRIVATE_KEY",
				"why": "Private half of the intake sealed-box keypair, held only by the consumer, which is the sole reader of the metadata envelope."
			}
		}
	]
}
